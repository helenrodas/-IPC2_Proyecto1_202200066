class test():
    
    def evaluar_nodos(self,lista_nodos,amplitud):
        # print(lista_nodos)
        print(lista_nodos[i].attrib['A'])
            # for i in range(1, amplitud + 1):
            #     amplitud_nodo = lista_nodos[i][0].('A')
